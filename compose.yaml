services:
  web:
    image: ${IMAGE_NAME:-gitea.typist.cc/typist/agents:latest}
    pull_policy: always
    restart: unless-stopped
    ports:
      - "${APP_PORT:-3000}:3000"
    env_file:
      - .env
