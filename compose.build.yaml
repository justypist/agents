services:
  agents:
    image: agents:latest
    build:
      context: .
      dockerfile: Dockerfile
  exec-sandbox:
    image: agents-exec-sandbox:latest
    build:
      context: .
      dockerfile: Dockerfile.exec-sandbox
