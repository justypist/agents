## 1. 数据模型与会话状态

- [x] 1.1 为聊天会话增加后台 turn 状态字段和迁移，覆盖运行状态、当前用户消息标识、错误摘要和更新时间
- [x] 1.2 扩展 `StoredChatSession` 与会话查询/保存服务，返回并更新后台执行状态
- [x] 1.3 为会话状态读写和迁移后的默认值补充单元测试

## 2. 后台执行服务

- [x] 2.1 提取可复用的 agent 回复生成逻辑，使其不依赖 HTTP 流响应或 `request.signal`
- [x] 2.2 实现提交聊天 turn 的服务：校验消息、持久化用户消息、设置运行中状态并执行幂等检查
- [x] 2.3 实现后台执行完成写入：生成 assistant 消息、保存最终消息、更新完成状态并保留标题生成行为
- [x] 2.4 实现后台执行失败写入：记录失败状态和错误摘要，并保留已提交用户消息
- [x] 2.5 为成功、失败、重复提交和运行中拒绝场景补充服务层测试

## 3. API 与前端恢复

- [x] 3.1 将聊天提交 API 改为接受 turn 并立即返回会话快照，不再把后台执行绑定到前端连接
- [x] 3.2 更新聊天页面发送逻辑，提交后展示后台运行状态并避免把断开当作执行失败
- [x] 3.3 在会话运行中时定期刷新或重新获取会话快照，完成后展示最终 assistant 回复
- [x] 3.4 在会话失败时展示持久化错误提示，并允许用户继续查看历史消息

## 4. 验证

- [ ] 4.1 补充 API 或组件测试，覆盖重新打开运行中、已完成和失败会话的展示行为
- [ ] 4.2 补充端到端或集成测试，模拟提交后连接断开并验证后台完成后可恢复结果
- [ ] 4.3 运行 `pnpm lint`、`pnpm typecheck` 和相关测试，修复发现的问题
